{
  "name": "blacksmith",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --port=3000",
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.2",
    "@11ty/eleventy-img": "^3.1.0",
    "sharp": "^0.34.3"
  }
}
